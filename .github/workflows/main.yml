name: CI

on: push

jobs:
  build:
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - run: mkdir -p dist && echo "hello" > dist/app.txt
      - uses: actions/upload-artifact@v4
        with:
          name: app-build
          path: dist
